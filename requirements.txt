# EmotiCore - Root Requirements
# For running the backend, install from backend/requirements.txt instead.
# This file is kept for project-level tooling references.

# Install backend: pip install -r backend/requirements.txt
# Install frontend: cd frontend && npm install

# Pipeline-only extras (for training/preprocessing scripts):
kagglehub
